import { Box } from '@chakra-ui/react';
import { FC } from 'react'

type AddressItemsProps = {
    abc?: any
};
const VoucherItems: FC<AddressItemsProps> = () => {
    return (
        <Box>

        </Box>
    );
};

export default VoucherItems 