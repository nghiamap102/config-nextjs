import { Box } from "@chakra-ui/react";
import { mainColor } from "@theme/theme";

type LoginHookFormProps = {

};

const LoginHookForm = (props: Props) => {


    return (
        <Box>
            
        </Box>
    );
};

export default LoginHookForm