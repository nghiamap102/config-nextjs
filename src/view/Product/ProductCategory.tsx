import { FC } from "react";

type ProductCategoryProps = {

};

const ProductCategory: FC<ProductCategoryProps> = (props: Props) => {
    return (
        <div>

        </div>
    );
};


export default ProductCategory